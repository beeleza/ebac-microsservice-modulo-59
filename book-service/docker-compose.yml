services:
  db:
    image: postgres:16-alpine
    container_name: books-service-db
    environment:
      POSTGRES_DB: books_service
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
